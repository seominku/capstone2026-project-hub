# Capstone2026 개발 체크리스트

| 항목 | 내용 |
|---|---|
| 최종 갱신 | 2026-08-24 |
| 상태 표기 | `[x]` 완료 / `[ ]` 대기 / `[-]` 스코프 제외 |
| 우선순위 | P0 필수 · P1 핵심 · P2 중요 · P3 품질 · P4 폴리싱 · P5 여유 시 |
| 세부 데이터 | Unity `Assets/Editor/DevChecklist/DevChecklist.asset` |

## 현재 스프린트

- [ ] **P0 `story-chapter-label`** — `B1~B9` 표시를 `1장~9장`으로 교체한다.
  - 완료 조건: HUD·카드·배너·유품 등 모든 플레이어 노출 문구가 장 표기로 통일된다.
- [ ] **P0 `story-camp-exit-root`** — 캠프 첫 출구를 지상으로 옮기고 뿌리를 7장 진입점으로 배치한다.
  - 완료 조건: 새 게임 첫 이동이 지하로 순간 이동하지 않고 스토리 동선과 일치한다.
- [ ] **P2 `story-camp-graves`** — 캠프를 무덤 4개에서 시작하게 한다.
  - 완료 조건: 새 세이브 첫 진입에서 4개, 사망 후에는 누적 수와 시각적으로 구분된다.
- [ ] **P2 `story-recorder-animal`** — 정령의 손 역할을 하는 기록자 동물을 배치한다.
  - 완료 조건: 초반에는 관찰자로, 심층에서는 뒤틀린 동일 계열 적으로 읽힌다.
- [ ] **P4 `story-tutorial-text`** — 탑 기준 문구를 뿌리 기준으로 교체한다.
  - 완료 조건: 삭제된 탑 설정이 게임 내 텍스트에 남지 않는다.

## 보스·엔딩

- [ ] **P0 `boss-model`** — 「잠들지 않는 뿌리」 3D 모델 제작·리깅.
  - 완료 조건: 반고정형 실루엣, 회전·지하 이동·코어 노출에 필요한 본 구조를 갖춘다.
- [ ] **P1 `boss-phase12`** — 1페이즈 근접과 2페이즈 투사체/소환 패턴 구현.
  - 완료 조건: Q 락온을 유지하면서 흐름을 쌓고 회피할 수 있다.
- [ ] **P1 `boss-phase3`** — 코어 노출·광역 뿌리 강타·강화 슈퍼아머 구현.
  - 완료 조건: 코어 노출이 서사의 “허락”으로 읽히고 마무리 타이밍이 명확하다.
- [ ] **P1 `ending`** — 보스 사망 이벤트, 숲 정화, 자리 계승, 크레딧 연결.
  - 완료 조건: `VictoryZone` 임시 도달 판정이 실제 보스 사망 판정으로 대체된다.

## 회귀 테스트

- [x] **P0 `death-memory-starter`** — 사망 후 기억 순서 유지, 기본 단검 지급, 유지 후보 제외, 미선택 진행.
- [x] **P1 `input-lockon-q`** — Q 락온, 락온 대상 기억 돌진, 방향 회피, 카메라 개선.
- [ ] **P1 `lockon-strafe-fix`** — 락온 후 좌우·후방 스트레이프 이동 방향과 애니메이션을 수정한다.
  - 완료 조건: 락온 대상을 계속 바라보면서 A/D는 원형 횡이동, S는 후진하고, 블렌드 방향과 실제 이동 방향이 일치한다.
- [x] **P1 `potion-audio-loot-qa`** — F 포션 입력, 몬스터 3D 발소리 조건, 드랍 경로 확인.
- [ ] **P0 `regression-death-second-run`** — 사망 → 다음 런 → 재사망까지 2회 연속 검증.
  - 완료 조건: `SmallAttack` 폴백, ATTACK 상태 고착, 장비 중복, 입력 비활성 문제가 없다.
- [ ] **P1 `regression-monster-animation`** — 몬스터 종류별 소실·지면 침투·공격 트레일 검증.
  - 완료 조건: 각 근접/원거리/대형 적을 2회 이상 상대해 위치 점프와 트레일 이탈이 없다.
- [ ] **P2 `regression-audio-headphones`** — 헤드폰으로 적 위치·거리·사망 후 잔류 발소리 확인.
  - 완료 조건: 소리 방향이 화면/월드 위치와 일치하고 비활성·사망 적의 발소리가 멈춘다.
- [ ] **P2 `regression-loot-sample`** — 일반/네임드 처치 드랍을 반복 샘플링한다.
  - 완료 조건: 미드랍 버그가 없고 등급·포션 확률이 기획 체감 범위에 들어온다.
- [ ] **P2 `regression-drop-readability`** — 다섯 등급 드랍 모델과 VFX 가독성을 재검증한다.
  - 완료 조건: 무기가 VFX에 가려지지 않고 에픽/유니크/레전더리를 10m 밖에서 구분한다.

## 장비·드랍 폴리싱

- [x] **P2 `arena-debug-panels`** — F2 몬스터 시험장, F3 장비 드랍, 패널 중 카메라 차단.
- [x] **P2 `equipment-drop-presentation`** — 장비 아이콘 제거, 세운 모델, 무기만 부유, 등급 VFX.
- [x] **P2 `weapon-grip-normalization`** — 물리 무기 15종 기본 위치·방향과 콤보 그립 합성 점검.
- [ ] **P3 `memory-icons`** — 플레이스홀더 기억 아이콘을 공격 특성이 읽히는 정식 아이콘으로 교체한다.
- [ ] **P4 `drop-vfx-final-pass`** — 실제 맵 조명/포스트프로세싱에서 투명도와 Bloom을 최종 조정한다.

## 플레이어 공격 표현

- [x] **P1 `player-attack-tempo`** — 기억별 배속을 보존하면서 모든 플레이어 공격의 최종 재생 속도에 공통 ×2를 적용했다.
- [x] **P1 `player-slash-vfx-hd`** — 브러시 참격을 크기 5.1로 적용하고 역할별 셀 512 시트, 타격별 회전, 0.60초 풀 재사용을 검증했다.
- [x] **P2 `dungeon-modular-authoring`** — 던전 모듈 6종을 Blender로 가져와 바닥·계단·아치 비파괴 편집 흐름을 준비했다.

## UI / UX 개선

- [x] **P2 `ui-theme-wood-v2`** — 로우폴리 숲 분위기의 나무 프레임·패널·슬롯 공통 테마를 적용했다.
- [x] **P1 `ui-management-inventory`** — 캐릭터 관리 화면, 장비 슬롯, 인벤토리, 선택 정보와 드래그 장착/해제를 연결했다.
- [x] **P1 `ui-memory-loadout`** — 보유 기억·선택 정보·장착 슬롯을 1920×1080 관리 화면에 배치하고 캐릭터 관리 탭과 왕복하게 했다.
- [x] **P2 `ui-slot-interaction`** — 인벤토리/장비 슬롯 클릭 상세 정보, 버튼 호버 확대, 빈 장비 부위 실루엣과 포인터 입력 우선순위를 정리했다.
- [x] **P1 `ui-customization-grid-preview`** — 왼쪽 부위 선택, 오른쪽 파츠 그리드·색상, 장비 미착용 기본값과 캐릭터 전용 미리보기를 적용했다.
- [x] **P2 `ui-unlock-panel`** — 장비 해금 카테고리·목록·선택 정보·정수 비용·진행도를 공통 테마로 구성했다.
- [x] **P1 `ui-pause-options`** — 한국어 일시정지 메뉴와 옵션 화면을 연결하고 옵션 진입 시 검은 화면 문제를 수정했다.
- [x] **P1 `ui-difficulty-flow`** — 난이도 선택 화면을 개편하고 쉬움 흰색·보통 노란색·어려움 빨간색의 선택 피드백과 다음 흐름을 복구했다.
- [x] **P1 `ui-gameplay-hud`** — HP, 포션, 골드, 정수, 미니맵 배경과 마름모 아이콘 정렬을 정리했다.
- [x] **P1 `ui-minimap-bigmap`** — 미니맵과 `M` 전체 지도, Fog of War, 지도 입력 흐름을 공통 디자인으로 개편했다.
- [x] **P1 `ui-death-flow`** — 사망 결과와 장비 최대 1개 유지/미선택 화면, 캠프 복귀 페이드를 개편했다.
- [x] **P1 `ui-camp-camera-rebind`** — 사망 후 캠프 카메라와 커스터마이징 미리보기 카메라/대상을 씬 재로드 뒤 다시 연결했다.
- [x] **P2 `ui-localization-table`** — 한국어/영어 문자열 테이블과 런타임 바인더를 만들고 현재 UI 언어를 한국어로 통일했다.
- [ ] **P1 `ui-full-run-regression`** — 캠프→난이도→런→일시정지/옵션→사망→캠프를 2회 반복하며 모든 주요 UI를 검증한다.
  - 완료 조건: 1920×1080과 다른 16:9 해상도에서 프레임·글·아이콘·슬롯 겹침이 없고 패널 전환 뒤 카메라/게임 입력이 새지 않는다.
- [ ] **P3 `ui-final-art-pass`** — 기억 정식 아이콘과 화톳불 뿌리 제단 등 남은 플레이스홀더 아트를 교체한다.

## 배포 전 필수

- [ ] **P0 `gen-seed-record`** — 절차 생성 시드를 저장·로그에 남긴다.
- [ ] **P0 `gen-path-verify`** — 생성 뒤 스폰→출구 NavMesh 경로를 검증하고 실패 시 재생성한다.
- [ ] **P0 `save-version-migrate`** — `SaveData.Version` 기반 마이그레이션과 백업을 구현한다.
- [ ] **P0 `build-stabilize`** — Windows 전체 런 2회, 오류 0건, 세이브 왕복을 통과한다.
- [ ] **P4 `warn-pushdoor-panel`** — `Wall_12_Door_01`의 PushDoor 패널 경고를 제거한다.

## 폴리싱·선택 작업

- [ ] **P2 `river-flood-stage`** — 여울목 범람/수위 상승과 다음 스테이지 침수 연결.
- [ ] **P3 `sound-assets-mix`** — 합성 임시음과 공용 몬스터 음원을 최종 음원으로 교체하고 믹싱한다.
- [ ] **P4 `bonfire-reskin`** — 화톳불을 세계관의 뿌리 제단 계열 비주얼로 교체한다.
- [ ] **P3 `cs-anim-tuning`** — 신규 기억 12종의 타이밍과 그립/트레일을 수동 조정한다.
- [-] **P5 `escape-floor`** — 변칙 탈출층. 핵심 일정이 밀리면 제외 유지.
- [-] **P5 `miniboss-dragon`** — 미니보스 드래곤. 보스와 엔딩 이후에만 재검토.
- [-] **P5 `cursed-weapons`** — 보물방 전용 저주 무기. 현재 스코프 제외.
- [-] **P5 `dungeon-preset`** — BSP 대형 공동 프리셋. 현재 스코프 제외.

## 문서 관리

- [x] **P4 `docs-refresh-0731`** — 기획서·스토리·구현 명세·현황·체크리스트·변경이력을 실제 상태로 동기화.
- [x] **P4 `docs-ui-refresh-0809`** — UI/UX 전면 개편과 사망 후 카메라 복구 내역을 기획서·상세기획서·현황·홈페이지에 동기화.
- [x] **P4 `docs-web-refresh-0824`** — 전투 템포·참격 VFX·Blender 모듈 편집 작업과 검증 이미지를 프로젝트 허브에 동기화.
- [ ] 기능 동작을 바꾸면 같은 작업 안에서 관련 `spec`과 `변경이력.md`를 함께 수정한다.
- [ ] 발표용 PDF는 Markdown 검토가 끝난 뒤 재출력한다.
- [ ] 프로젝트 허브 웹사이트는 이 체크리스트의 ID와 상태를 그대로 표시한다.
